apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  labels:
    app: postgres
    platform: raqeem
spec:
  resources:
    requests:
      storage: "2Gi"
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
---

