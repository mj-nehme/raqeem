NAMESPACE=default
DEVICES_BACKEND_PREFERRED_NODEPORT=30080
# Required if mentor-backend not started; otherwise discovered.
MENTOR_API_URL=

# Optional: override CORS origin regex for allowed frontends
# Default (if unset) allows any localhost port: ^http://localhost(:[0-9]+)?$
# CORS_ORIGIN_REGEX=
DATABASE_URL=postgresql+asyncpg://monitor:supersecret@127.0.0.1:5432/monitoring_db
# PostgreSQL database
POSTGRES_USER=monitor
POSTGRES_PASSWORD=supersecret
POSTGRES_DB=monitoring_db
POSTGRES_HOST=localhost
POSTGRES_PORT=30432

# Construct DATABASE_URL for SQLAlchemy Async
# Format: postgresql+asyncpg://user:password@host:port/dbname
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# FastAPI server config (for CORS, etc)
FRONTEND_HOST=http://localhost
FRONTEND_PORT=4000

# Object storage (S3/MinIO) config (preferred BUCKET_*)
BUCKET_ENDPOINT=localhost:9000
BUCKET_ACCESS_KEY=minioadmin
BUCKET_SECRET_KEY=minioadmin1234
# Set to true if using https
BUCKET_SECURE=false
BUCKET_NAME=raqeem-screenshots

# Security
SECRET_KEY=some-very-secret-string
# Token expiration in minutes (7 days)
ACCESS_TOKEN_EXPIRE_MINUTES=10080
