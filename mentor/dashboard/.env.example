# Mentor frontend environment variables
# Leave empty when using the repository root .env and scripts/start.sh
# VITE_MENTOR_API_URL will be exported by the start script based on your configured ports.
# VITE_MENTOR_FRONTEND_PORT can be provided at runtime (env) if you want to override.
