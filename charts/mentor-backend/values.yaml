replicaCount: 1

image:
  repository: raqeem/mentor-backend
  tag: v1.1.1
  pullPolicy: Never

service:
  type: NodePort
  port: 80
  targetPort: 8080
  nodePort: 30090

resources: {}

postgres:
  host: postgres-service.default.svc.cluster.local
  port: 5432
  db: monitoring_db
  user: monitor
  passwordSecret:
    name: postgres-secret
    key: POSTGRES_PASSWORD

minio:
  endpoint: http://minio-service.default.svc.cluster.local:9000
  # Public endpoint for presigned URLs (accessible from browser)
  # If not set, uses the internal endpoint (which won't work from browser)
  publicEndpoint: http://localhost:30421
  region: us-east-1
  bucket: raqeem-screenshots
  accessKeySecret:
    name: minio-secret
    key: accesskey
  secretKeySecret:
    name: minio-secret
    key: secretkey

# CORS configuration - frontend origin(s)
# Can be a single origin or comma-separated list
# Default development ports that start-smart.sh typically detects
frontendOrigin: "http://localhost:5000,http://localhost:5001,http://localhost:5002"

# Optional: CORS regex pattern to match allowed origins (takes precedence over frontendOrigin)
# Example: "^http://localhost:(4000|4001|4002|4003|4004|5000|5001|5002|5003|5004)$"
# This allows dynamic ports within a limited range, solving CORS issues with port discovery
frontendOriginRegex: ""

# Optional: Devices API URL for forwarding commands/results to devices backend
# Example (dev): http://localhost:30080/api/v1
devicesApiUrl: ""

nodeSelector: {}

tolerations: []

affinity: {}
